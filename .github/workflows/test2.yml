name: gitea-actions
run-name: ${{ github.actor }} is hero
on:
  workflow_call:

jobs:
  check-zip-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
#      - uses: actions/setup-node@v4
#        with:
#          node-version: '14'
#      - run: npm install -g bats
      - run: zip -v
